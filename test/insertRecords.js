use ambience
db.test.insert({"Test":"ambience, it works!"})
use ambience-logs
db.test.insert({"Test":"ambience-logs, it works!"})
use ambience-temp
db.test.insert({"Test":"mbience-temp, it works!"})
use eno
db.test.insert({"Test":"eno, it works!"})