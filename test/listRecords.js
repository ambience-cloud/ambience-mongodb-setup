use ambience
db.test.find({"Test":"ambience, it works!"})
use ambience-logs
db.test.find({"Test":"ambience-logs, it works!"})
use ambience-temp
db.test.find({"Test":"ambience-temp, it works!"})
use eno
db.test.find({"Test":"eno, it works!"})
